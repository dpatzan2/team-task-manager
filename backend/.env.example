# Copy to .env and export before running in anything other than local dev.
# settings.py reads these from the environment; the defaults are dev-only.
DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=example.com,www.example.com
